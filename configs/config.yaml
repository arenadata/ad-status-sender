adcm_url: "https://adcm.example.com"
host_id: 101

token_file: "/etc/secure/adcm.token"
# adcm_user: "admin"        # use with adcm_password instead of token
# adcm_password: "secret"

# rules source: yaml | legacy | adcm
rules_source: "yaml"
rules_path: "/etc/ad-status-sender/conf/rules.yaml"
rules_db: "/var/lib/ad-status-sender/rules.db"
# legacy_dir: "/usr/lib/adcm-status-checker"
# rules_refresh_interval: "60s"  # for rules_source: legacy | adcm

interval: "5s"
http_timeout: "5s"
concurrency: 0 # (0 = autodetect)
log_bodies: false
force_send_after: "240s"

log_level: "info"   # one of: debug, info, warn, error
log_format: "text"  # or: json

# File logging with in-process rotation. Leave log_file empty to log to stdout
# (captured by journald). When set, logs go to the file and are rotated.
# log_file: "/var/log/ad-status-sender/ad-status-sender.log"
# log_max_size_mb: 100    # rotate once the file exceeds this size (default 100)
# log_max_backups: 7      # rotated files to keep (default 7)
# log_max_age_days: 28    # delete rotated files older than N days (default 28)
# log_compress: true      # gzip rotated files (default true)

tls:
  ca_file: "/etc/pki/ca-trust/source/anchors/adcm-root.pem"  
  cert_file: "/etc/ad-status-sender/client.crt"               
  key_file: "/etc/ad-status-sender/client.key"                
  server_name: "adcm.internal"                               
  insecure_skip_verify: false                                  
